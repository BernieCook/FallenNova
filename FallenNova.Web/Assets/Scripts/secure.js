$(function () {

    $('[rel=tooltip]').tooltip();

    // TODO: Wire up search result "View" select lists onchange event here. This is progressive enhancement (thanks to unobtrusive JavaScript).
    // $("#ddlView?").onchange(do post);
    // Remove submit button associated with the view. Don't just hide it, remove it.
});